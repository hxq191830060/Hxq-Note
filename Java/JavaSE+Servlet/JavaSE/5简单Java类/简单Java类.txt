简单Java类————可以描述某一类信息的程序类
并且简单Java类中没有特别复杂的逻辑操作，只作为一种信息存储媒介

简单Java类，核心开发结构如下
1，类名称一定要有意义，可以明确的描述某一类事务
2，类之中所有属性必须private进行封装，同时封装后的属性必须提供getter和setter
3，类中可以有无数个构造方法，但必须保留无参数构造方法
4，类中不可以出现任何输出语句，所有内容的获取必须返回
5，(非必须)可以提供一个获取对象详细信息的方法，暂时将此方法名称定义为getInfo()